# ============================================================================
# MindPolis: scoring-service/requirements.txt
# Version: 1.0.0 — 2026-03-07
# Why: Python dependency manifest for the scoring microservice.
#      Pin major versions for reproducibility across dev and prod Docker builds.
# ============================================================================

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Data validation
pydantic==2.9.0
pydantic-settings==2.5.2

# Scientific computing — the core scoring math stack
numpy==2.1.0
scipy==1.14.0

# Machine learning — for future clustering and prediction models
scikit-learn==1.5.2

# HTTP client — for future inter-service calls if needed
httpx==0.27.0

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
httpx==0.27.0
